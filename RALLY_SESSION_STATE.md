# Rally — Session State & Learnings

**Última actualización:** v0.3.09 (+fix de cache de Hosting) — 2026-07-06, sesión remota (Claude Code on the web).
- **Bug "sigue diciendo 100" (foto de Lucio con v0.3.09 en pantalla):** descartado el código (validado 35/35 en local) y descartado el override remoto (`config/maxHp` — el admin muestra 35, sin override). Causa raíz: **Firebase Hosting sin headers de cache = default `max-age=3600` para TODO, incluido `index.html`** — el navegador retiene hasta 1h el HTML viejo, que carga el game.js viejo por su `?v=` viejo (el cache-busting solo protege JS/CSS si el HTML está fresco). Fix en `firebase.json`: `**/*.html` → `Cache-Control: no-cache` (siempre revalida; los deploys llegan al instante) y `js/css/png` → `max-age=604800` (seguro porque cada versión cambia el `?v=`). Nota: el fix rige para requests FUTURAS; la primera vez puede hacer falta un force-refresh para soltar el HTML ya cacheado.
- **v0.3.09 — fix reporte de Lucio "antes de arrancar el HP dice 100":** los `<span id="hp-num-you/opp">` del HUD tenían "100" hardcodeado como placeholder en index.html, visible el instante previo a que `updateHud()` pinte los valores reales (con maxHp 35 el mismatch se notaba). Ahora arrancan vacíos. El "100 HP" de `#bracket-you-hp` (bracket de torneo) queda: es correcto ahí y `showTourneyBracket()` lo pisa siempre antes de mostrarse.
- **⚡ v0.3.08 — Partidas rápidas (35 HP) + serie "mejor de 3" default online + puntos de ronda + favicon:**
  - **`CFG.maxHp` 100→35** (partidas más rápidas, pedido de Lucio). **El Torneo offline queda AFUERA**: nueva const `TOURNEY_YOU_HP=100` (jugador arranca el torneo con 100; rivales siguen con su curva 10→200). Para que HUD/curas/IA escalen bien con máximos mixtos, `G.you` ahora lleva `maxHp` propio (igual que ya hacía `G.opp`) y TODOS los sitios que dividían/clampeaban por `CFG.maxHp` del lado del jugador usan `G.you.maxHp||CFG.maxHp` (updateHud, barras de showDuelOutcome, `cpuDuelAdvantage.hpEdge`, curas de anillo con `player.maxHp`, valoración IA del anillo con `G.opp.maxHp`). Campaña: default 35 salvo `youHp`/`hp` explícitos del nodo. **Anillo re-escalado** (estaba calibrado a 100 y quedaba degenerado): ringBigHeal 50→18, ringHealDiff 20→7, ringHealUnder 40→14, ringDripHeal 5→2. Espejo `PARAMS` de admin: maxHp 35.
  - **Serie online = "mejor de 3", default:** `BO5_TARGET` 3→2 (el valor interno del modo sigue siendo `'bo5'` a propósito — no se toca el wire ni `database.rules.json`). `App.matchMode` default `'bo5'`, `is-on` inicial en `#mode-bo5`, labels "Mejor de 3"/"llegar a 2", `TEXTS.resultBo5Eyebrow` "Mejor de 3 · ..." (+ espejo `TEXT_PARAMS` admin). OT (torneo x4) sigue forzando `'single'` por partido (línea en `OT.beginMatch`, intacta).
  - **Puntos de ronda:** nuevo `#round-dots` en `#screen-result` (entre título y score) + `renderRoundDots()`: `SERIES_ROUNDS=3` puntos en orden CRONOLÓGICO de rondas (gris = pendiente, azul `--accent` = la ganaste, negro `--ink` = el rival; los empates no pintan punto). Nuevo `App.roundHist` ('you'/'opp'), pusheado junto al score en `endGame()` y reseteado en los 3 mismos lugares que `scoreYou/scoreOpp` (fin de serie + los dos "nueva serie"). `endGame()` limpia `#round-dots` arriba de todo (como `#tourney-progress`); CSS `:empty{display:none}`.
  - **Favicon:** `public/favicon.png` (PNG 48x48 subido por Lucio) + `<link rel="icon">` y `apple-touch-icon` en index.html con `?v=` (el bump por sed los mantiene).
  - Validado con Playwright: rápida offline 35/35 en HUD; torneo bracket "100 HP", partida 100 vs 10 (Maurice); modo default `bo5`; serie simulada vos→rival→vos = puntos azul/negro/azul, eyebrow "Mejor de 3 · 2–1", "🏆 Ganaste la serie" al llegar a 2, y reset de score/hist post-serie.
- **v0.3.07 — limpieza del menú principal + título de pestaña:** borrado el `<p class="brand__tag">` del home ("Movete por el tablero…"); el layout se reacomoda solo (`.screen--home` centra por flex). ⚠️ La regla CSS `.brand__tag` NO se borra: la sigue usando la pantalla Offline ("Jugá sin conexión contra la máquina."). El `<title>` pasó de "Rally — duelo en tablero · vX" a **"Rally beta - vX"** (recordar que el bump por sed `s/vX/vY/g` sobre index.html lo mantiene solo). `#info-ver` sigue diciendo "alpha" (no se pidió tocar). Verificado con screenshots (home y offline, título de pestaña OK).
- **v0.3.06 — la onda de aviso de duelo espera al movimiento:** la onda ahora arranca con `setTimeout(..., 400)` (2 frames + .35s = duración del deslizamiento FLIP de `flipMarker()`), para no pisar la animación de movimiento hacia los casilleros. Además, el `setTimeout` que dispara el duelo pasó de 550ms fijo a `willDuel ? 1150 : 550` para que la onda (0.7s desde los 400ms) se vea completa antes del countdown — determinista (ambos clientes calculan el mismo `willDuel` del mismo estado), así que online sigue sincronizado. El haptic del aviso también se movió adentro del timeout. Validado con Playwright: sin `.duel-fx` a los 200ms (FLIP corriendo), onda presente a los ~600ms, duelo arranca después.
- **⚔️ Aviso visual de duelo inminente (casillas contiguas, v0.3.04-05):** cuando ambos terminan el movimiento en casillas contiguas (misma condición exacta que dispara el duelo: `areAdjacentOrSame` + `!wallSeparates` + `!wasTruce`, y `!willClash` porque misma-casilla ya tiene su clash-fx), en `resolveMoves()` se dispara una **onda expansiva grande AZUL (`--accent`, misma estética que `.clash-fx`) centrada en el PUNTO MEDIO entre las dos casillas** (variante elegida por Lucio sobre "una onda por casilla"). El `.duel-fx` se appendea al `#board` (no a una celda — el punto medio puede caer en el borde entre celdas; `.board` ahora tiene `position:relative`); JS calcula centro/tamaño en px con `getBoundingClientRect()` de las dos celdas (a prueba del espejo del guest y de adyacencia diagonal; celdas ubicadas por `data-x/data-y` canónicos). Reutiliza el keyframe existente `clashShock` (0.7s); limpieza sola en el próximo `renderBoard()` (mismo patrón que clash-fx); guard de `prefers-reduced-motion`. **Feedback v0.3.05:** la v0.3.04 salió en rojo (`--bad`) + marco pulsante `is-duel-warn` en ambas casillas — Lucio lo corrigió: NADA de iluminar casilleros en rojo, la onda va azul igual que la del choque en misma casilla (se eliminó `is-duel-warn` por completo). Validado con Playwright en ambas versiones: onda centrada entre las fichas mid-animación, duelo arranca después, sin fx residual visible.
- **🎮 EDITOR DEL JUEGO en `/admin/` (pedido: crear contenido sin IA a futuro).** Tres secciones visuales nuevas en el panel, mismo patrón nube que config/texts (nodos RTDB lectura pública + escritura solo-admin, todo como **JSON string** atómico):
  - **🗺️ Editor de mapas** (`maps/{id}` = JSON `{name,size,board,walls}`): grilla clicable 7x7 clásico o 9x9 con paredes (9x9 juega con reglas de Modo Paredes). Paleta de ítems (vaciar/🗡️/◈/×/💍); en 9x9 los BORDES entre casillas togglean paredes al tocarlos. Esquinas R/V bloqueadas (posiciones de arranque). Al guardar: BFS de conectividad (espejo compacto de `Walls.key/blocks/_fullyConnected` — mantener sincronizado con game.js) rechaza mapas con casillas inalcanzables.
  - **📖 Editor de campaña** (`campaign/script` = JSON array de nodos): tarjetas por nodo (escena=textarea de líneas / partida=rival nombre/hp/skill/color/dmgMult/youHp + selector de mapa), subir/bajar/borrar/agregar, export/import JSON avanzado. Al guardar, el mapa elegido se EMBEBE en `node.board` (string serializado; `node.mapId` queda solo como bookkeeping del editor) — el juego NO lee `maps/`. ⚠️ Si se edita un mapa hay que re-guardar la campaña para que tome la versión nueva (documentado en el hint).
  - **🎭 Editor de personajes** (`characters/roster` = JSON array de 8): nombre/emoji/color/trait/hp/skill por rival del torneo; campos hp/skill vacíos = curva exponencial default (mostrada como placeholder/referencia).
- **Lado juego (game.js):** `applyRemoteCampaign()` reemplaza `CAMPAIGN_SCRIPT` entero si `campaign/script` es válido (flag `Campaign._remoteScript` evita que `TEXTS.campaignOpp1Name` pise el nombre del nodo 0 del editor); `applyRemoteCharacters()` mergea campo a campo en `TOURNEY_ROSTER` (si trae name, sincroniza `TEXTS.rosterName{i}`; un override en texts/ sigue ganando); `tourneyHpFor/SkillFor` prefieren `r.hp`/`r.skill` del editor sobre la curva. **Tablero fijo por nodo:** en `startGame()`, si el nodo de campaña trae `board`, se usa `deserializeBoard()` (resuelve formato clásico y "W<size>~paredes~celdas", setea boardSize/wallsMode) en vez de `buildBoard()`; esquinas vaciadas defensivamente; si el nodo SIGUIENTE no trae board y quedó wallsMode del anterior, `exitSpecialMode()` antes de generar.
- **Reglas nuevas** (`database.rules.json`): `campaign/script` (string ≤200k), `maps/$id` (id `[A-Za-z0-9_-]{1,40}`, string ≤8k), `characters/roster` (string ≤8k) — todos `.read:true` + write solo-admin + `$other:false`. Validadas contra el emulador (lectura sin auth OK, write sin auth/no-admin rechazado, admin OK, campos extra rechazados).
- **E2E completo con emulador + bundles npm locales** (scripts `serve.js` + `e2e-editor.js` en scratchpad — recrear si hace falta; serve reescribe gstatic→`/vendor/` e inyecta `useEmulator()` tras `firebase.database()`): admin loguea, crea mapa 9x9 con paredes (esquina bloqueada ✓, mapa desconectado rechazado ✓), campaña escena→partida(mapa)→escena, personajes con rival editado; el JUEGO carga todo y la partida de campaña se juega EXACTAMENTE en el mapa pintado (ítems+paredes+hp/skill/nombre del rival) — todo verificado por assertions, 0 errores de página.
- **Lab solo-admins:** `openLab()` ahora es async y gatea con `isAdmin()` (nuevo helper, lee `admins/{uid}` — las reglas ya permitían leer la clave propia, sin cambios en `database.rules.json`; memoizado en `_isAdminCache` solo en éxito). Los dos accesos ocultos (`?lab=1` y 5 taps en la versión) quedan igual pero rebotan con toast (`TEXTS.toastLabAdminsOnly`) si no sos admin. En DEMO/sin Firebase el lab queda inaccesible (semántica pedida).
- **Animación de choque en la misma casilla:** en `resolveMoves()`, si `willClash`, se agrega a la celda `.is-both-here` la clase `is-impact` + un `<div class="clash-fx">` (anillo expansivo one-shot, ~0.55s — encaja en la ventana de 550ms antes del duelo) y haptic más marcado; las fichas aterrizan con un pop (`clashLandYou/Opp`, terminan en el transform de reposo de `.is-clash`). El próximo `renderBoard()` limpia todo solo (mismo espíritu que `spawnStartBubble`). Con guard de `prefers-reduced-motion`.
- **Fix regresión v0.3.00 — botón de tema pisaba la barra de vida en partida:** el flotante (`#btn-theme`, top-right absoluto) quedaba sobre la tarjeta derecha del HUD en `#screen-game`. Ahora `show()` lo oculta SOLO en `screen==='game'` y el pie de partida tiene su propio toggle `#btn-theme-game` (`.icon-btn` junto a `♪ on`/`salir`, label `☾`/`☀` actualizado por `applyTheme()`). Handler compartido extraído a `toggleTheme()`. El tema sigue cambiable en cualquier pestaña.
- Validado con Playwright headless: lab bloqueado por ambos accesos sin admin (con toast) y abierto con `_isAdminCache=true`; flotante oculto en juego + toggle del footer alterna tema y no pisa `.hud-bar` (bounding boxes + screenshots claro/oscuro); clash fx presente al caer ambos en la misma casilla y limpiado tras el próximo render.
- (Anterior en v0.3.01:)
- **Pantalla de ganador/perdedor de torneo (offline y online), mejorada.** Antes era la parte menos pulida de la app: la rama de `Tourney` (offline, roster de 8) en `endGame()` (`game.js`) era la única de las 4 ramas que nunca aplicaba `is-win`/`is-lose` al título (fix de consistencia); tampoco había sonido/haptic en ese momento (sí en el duelo puntual) ni ningún recap de progreso/HP.
  - Nueva clase `.result-title.is-champion` (dorado, mismo `#D4AF37`/`#9A7B14` que "Rey Julian"/`is-king`) para el momento de campeón, offline y online.
  - Nueva animación de entrada `.result-title.is-pulse` (`pulseResultTitle()`, saca+pone la clase con reflow forzado porque una animación CSS no se reinicia sola), disparada en cada resultado de torneo. Ambas respetan `prefers-reduced-motion`.
  - `Sound.win()/lose()` + `haptic(...)` (mismos valores que ya usa el duelo puntual) en cada resultado de torneo, offline y online. En el hub online (`OT.renderHub()`, que se re-renderiza seguido por listeners de Firebase) se gatea con `OT._resultSoundPlayed` para no repetir el sonido en cada re-render — se resetea junto con `_champBumped` en `resetRunFlags()`/`route()`.
  - Torneo offline: nueva fila de chips `#tourney-progress` (`renderTourneyProgress()`) en `#screen-result` con los 8 rivales del roster (vencido/actual/rey), mismo criterio que `showTourneyBracket()` pero horizontal; vacía y oculta (`:empty{display:none}`) fuera de Tourney.
  - Torneo online: `OT.onMyMatchEnd(youHp, oppHp)` ahora guarda `_lastYouHp/_lastOppHp` y `renderHub()` los muestra como recap (reusa `TEXTS.resultScoreHp`, el mismo texto que ya usan las otras pantallas de resultado) en campeón/final perdida/eliminado/semifinal ganada.
  - Validado con Playwright headless llamando directo a `endGame()`/`OT.onMyMatchEnd()`/`OT.renderHub()` con estado simulado (sin partida real): clases correctas en los 5 estados, chips de progreso correctos, recap de HP presente donde corresponde, sonido gateado a una sola vez con re-renders repetidos.
- Sesión anterior (v0.3.00, resumen): footer movido a `screen-info` (antes en home) + texto en minúscula; botón de tema visible en cualquier pestaña (antes solo en home); 5 bugs de red arreglados (logout `ReferenceError`, doble `listenStart()` en revancha/bo5, `stopListenStart()` pisando el listener de `listenBoard()` — mismo path `game/board`, chat sin soltar listener de ronda anterior, `updateNeedles()` re-consultando DOM cada frame del duelo).
**Idioma:** todo con el usuario (Lucio) en español argentino.
Memoria entre sesiones: mantener COMPACTO (el usuario cuida tokens — condensar/borrar lo viejo al agregar secciones nuevas).

## REGLAS FIJAS DE WORKFLOW (no negociables)
1. **Mergear SIEMPRE automáticamente** al entregar un update — no dejar el PR abierto esperando aprobación.
2. **No entregar HTML fusionado.** Con el merge a `main` alcanza: el deploy automático a Firebase Hosting lo publica y el usuario prueba ahí directo.
3. **Actualizar este archivo (`RALLY_SESSION_STATE.md`) SIEMPRE al terminar cada actualización, ANTES de mergear** — el merge nunca va sin este archivo al día.
4. **Revisar/actualizar `database.rules.json` en la MISMA entrega** cuando el cambio toca algo que las reglas validan (forma de un nodo, campos nuevos, permisos) — no dejarlo para después. El deploy de reglas es automático (`.github/workflows/firebase-rules-deploy.yml`, corre en cada push a `main` que modifique `database.rules.json`), así que un fix de reglas se publica solo con el mismo merge.

## WORKFLOW (sesión remota)
- Se trabaja sobre los 3 archivos split en `public/` (index.html ~429 líneas, style.css ~1143, game.js ~3485), commits/push a rama `claude/...` y PR a `main`.
- Por versión: 1) editar, 2) bump `VERSION` en game.js + `<title>` y `.version-tag` en index.html **+ los `?v=` de `<script>/<link>` (cache-busting, desde v0.2.74 — clave o hay mismatch JS/HTML)**, 3) validar sintaxis:
  `node -e "const fs=require('fs');const js=fs.readFileSync('public/game.js','utf8');try{new Function('window','firebase','document','navigator','performance','requestAnimationFrame','cancelAnimationFrame','location','URLSearchParams',js);console.log('OK');}catch(e){console.log('ERR',e.message);}"`
- Screenshots: playwright-core (npm i en scratchpad) + Chromium en `/opt/pw-browsers/chromium`, viewport 390px.
- Cambios de matemática (daño/puntería/probabilidad): simular standalone en node ANTES de entregar (ver aisim v0.2.75).
- El usuario también edita en VS Code y a veces en chat/Design (entrega un `.html` fusionado). Si aparece uno: viene de Design, re-splitear (`grep -n "<style>\|</style>\|<script"` + sed + validar con `new Function`), y comparar VERSION contra la última conocida por si saltó versiones.

## COLABORACIÓN EXTERNA (revisado 2026-07-03)
Un amigo del usuario dijo haber creado "una rama". **Verificado en GitHub: no existe** — en `guerrasur/rally-alpha` solo hay `main` + ramas `claude/...`, único colaborador `guerrasur`. Probable fork sin push aún. Para integrar: si es fork → PR hacia `guerrasur/rally-alpha`; si quiere pushear directo → invitarlo como colaborador. Próxima sesión: pedir su usuario de GitHub o link, revisar PRs entrantes antes de asumir que no hay nada.

## DEPLOY
- **✓ Automático (desde 2026-07-02):** `.github/workflows/firebase-deploy.yml` deploya a Firebase Hosting en cada push/merge a `main`; secret cargado, runs verdes. Mergear a main = deploy (ya no depende de la Mac).
- **✓ Reglas de DB también automático (desde 2026-07-04):** `.github/workflows/firebase-rules-deploy.yml` corre `firebase deploy --only database` en cada push a `main` que modifique `database.rules.json` (mismo secret que hosting). Ya no hace falta publicarlas a mano desde la consola ni desde la Mac — con mergear alcanza.
- Site: **`rallyyy`** (`firebase.json` target `rally` → `.firebaserc` → `rallyyy`). `rallyyy-test` no se usa.

## FIREBASE
- Proyecto `rallye-online`, Realtime Database. En v0.2.78 se hicieron reglas definitivas + auth anónima; **desde v0.2.89 el deploy de `database.rules.json` es automático** (ver DEPLOY) — este merge es la PRIMERA vez que reglas reales (no test-mode) quedan activas en producción. Por eso valió la pena auditarlas a fondo antes de publicar (ver bugs de reglas más abajo): bajo test-mode nunca se hubieran notado.
- El proxy remoto bloquea `*.web.app`/`*.firebaseio.com` (no se puede testear RTDB en vivo desde acá) pero sí `identitytoolkit.googleapis.com` (auth verificable). Testear reglas SIEMPRE con emulador local (firebase-tools + Java disponibles). **⚠️ Namespace correcto del emulador: `rallye-online-default-rtdb`, NO `rallye-online`** (el `.firebaserc`/`firebase.json` usan el nombre corto, pero el emulador de RTDB namespacea con el sufijo `-default-rtdb`) — usar el namespace corto crea un namespace "fake" sin reglas y TODO pasa sin validar (falso positivo silencioso, así se me pasó el primer bug de este batch). Para simular un usuario autenticado por REST sin el SDK: `?ns=rallye-online-default-rtdb&access_token=owner&auth_variable_override={"uid":"..."}` (las tres juntas — `access_token=owner` solo no basta, ni `auth_variable_override` solo).
- **Auth anónima (v0.2.78):** `firebase-auth-compat`; `ensureAuth()` en game.js = único punto de entrada, memoizada, con retry; awaited en `Net.createRoom`/`Net.joinRoom` (cubre torneo OT). Invisible al jugador, persiste uid entre visitas. Futuro login real: `linkWithCredential` (conserva uid) — reglas y nodo `users/$uid` ya listos, hoy sin uso (progreso sigue en localStorage).
- **Bug #23 "se sale el login" (v0.2.82):** `firebase.auth().currentUser` es `null` hasta que Firebase RESTAURA la sesión guardada del disco (async, puede tardar segundos en celu/red lenta). Si `ensureAuth()` se llamaba en esa ventana (p.ej. abrir sala apenas carga la página) entraba anónimo y PISABA la sesión con contraseña. Fix: `ensureAuth()` espera el primer `onAuthStateChanged` antes de decidir (flag `_authSettled`, solo la primera vez); un listener global de `onAuthStateChanged` recupera `User.name` desde `users/$uid/username` si la caché local se perdió, y repinta el overlay/UI en cada cambio de auth. Validado con E2E (emulador + restauración demorada simulando red lenta), 5/5, script `e2e-session.js` en scratchpad.
- **Reglas (`database.rules.json`, en repo):** default deny; `rooms` legible con auth; `rooms/$code` escribible con auth + código `^[A-HJ-NP-Z2-9]{4}$` + `createdAt` no forjable a futuro (≤ now+60s); whitelist estricta de campos ($other → false), enums, límites (nombres ≤40, chat ≤200, board ≤200, x/y 0-8). `game/` duplicado para sala directa y torneo. Más `config/`+`admins/` para el panel de admin (ver MODOS). Sin auth por identidad de sala: cualquier autenticado escribe cualquier sala (statu quo, roles no verificables sin refactor grande).
- **Bug de reglas — `game/ejects` (v0.2.89):** `youPos`/`oppPos` exigían `newData.isNumber()`, pero `Net.pushEject()` (game.js) siempre escribió objetos `{x,y}` — roto desde que existe el feature de empate/reposición, invisible porque las reglas seguían en test-mode. Con reglas reales, **cualquier empate online se hubiera trabado para siempre** (guest se queda en "Reposicionando…", la escritura del host rebota rechazada). Fix: validar `youPos.x`/`youPos.y`/`oppPos.x`/`oppPos.y` como número 0-8 (mismo patrón que `moves`), en la copia de sala directa y en la de torneo.
- **Bug de reglas — suplantación en `players/$seat.user` (torneo, v0.2.89):** solo chequeaba que el username existiera (`root.child('usernames').child(newData.val()).exists()`), a diferencia de `host.user`/`guest.user` que además exigen `=== auth.uid`. Cualquier autenticado podía escribirse el username de OTRO jugador real en su propio seat de torneo. Fix: mismo chequeo de dueño que host/guest.
- **Gap conocido, no arreglado (bajo impacto, backlog):** ningún campo compuesto de esta ruleset (`moves.$turn.$role`, `ejects.$duelId.youPos/oppPos`, `spec.A/B`) fuerza que el valor sea REALMENTE un objeto — si se manda un escalar en vez de `{x,y}`, las reglas hijas simplemente no se disparan (no hay `newData.hasChildren([...])` a nivel del padre) y la escritura pasa. Es preexistente (estaba también en `moves` desde siempre, no es nuevo de este fix) y de bajo impacto (mismo modelo de confianza que "sin anti-cheat", ya aceptado). Si se quiere endurecer: agregar `newData.hasChildren(['x','y'])` a cada campo compuesto.
- Compat SDK v10.12.2. Flags HAS_FIREBASE/DEMO, global `fbDb`. Si auth no carga, `ensureAuth()` resuelve null y cae en los try/catch existentes.
- **Arquitectura online:** sala en `rooms/{código-4-chars}`, host crea/guest se une. Board lo genera el host (CELL_CODE: e/a/d/x/r; prefijo "W"=paredes), guest lo espeja 180° (`G.flip`+`viewCoord`, ambos se ven abajo-derecha). Moves en `game/moves/{turn}/{role}`, resolución idéntica en ambos; host limpia turno anterior. Duelo: push score+pos a `game/duels/{duelId}/{role}`, resolución determinista; empate/eject decide host. `Net` concentra todo; `Net.leave()` limpia listeners. Modo en `game/mode`; presencia en `presence/{role}` + onDisconnect (6s gracia). Chat en `rooms/{code}/chat` (push-id, limitToLast 50). **Lobby (v0.2.81, bug #22 "sala llena"):** el lugar del guest se libera solo por desconexión (`onDisconnect().remove()` armado en `joinRoom`, cancelado en `startPresence` — en partida manda la presencia); si el guest se va del lobby, el host vuelve a `waiting` (`Net.onGuestLeft`). Repro+fix validados con E2E emulador (6/6, script `e2e-join.js`+`serve.js` en scratchpad — recrear si hace falta; sirve `public/` con bundles npm locales y `useEmulator` inyectado). La sala del host abandonado sigue viva hasta la limpieza de 2hs (onDisconnect en host es riesgoso: backgroundear para compartir código la borraría).
- **Bug "revelado del duelo se corta" (fix v0.2.89):** `resolveDuelOnline()` se llama desde dos caminos (`commitMyDuelScore` si el rival ya frenó, y `onDuelScoresReady` cuando el listener de Firebase ve ambos scores) que pueden dispararse casi simultáneos cuando frenás SEGUNDO (ya sabías el score rival; tu propio push recién "rebota" un instante después). Antes, el segundo trigger reentraba a la misma función mientras `_revealShown` ya estaba en `true` y saltaba directo al veredicto — cortando el ~2.2s de pantalla de revelado a casi cero, solo para quien frenó segundo (asimetría visible entre los dos jugadores). Fix: separada en `resolveDuelOnline()` (entrada idempotente — un segundo trigger no hace nada si el paso 1 ya arrancó) + `finishDuelOnline()` (el veredicto real, alcanzable SOLO por su propio `setTimeout` de 2200ms, nunca por un trigger externo).
- **Auto-movimiento por inactividad (solo online, v0.2.96, pedido de Lucio):** en `startChoosePhase()`, si `G.online` y NO es un match de Torneo online x4 (`OT.active && OT.inMatch` queda afuera a propósito — ver más abajo), arranca `startIdleTimer()`: 6s totales antes de mover solo, invisible los primeros 3s. Barra `#idle-timer`/`#idle-timer-fill` (nueva, entre `.board-wrap` y `.turn-footer`) aparece al segundo 3 en verde, se achica con `transition:width` lineal durante los 3s restantes, pasa a amarillo al segundo 4, a rojo al segundo 5 (con brillo titilante `@keyframes idleDanger`, clase `.is-danger`). A los 6s sin elegir, `autoMoveIdle()` te mueve con `bestConvenientMove()` (heurística nueva y simple — NO reusa `cpuDecideMove()`, que está atada a conceptos de la IA offline como skill/rasgos/historial anti-vaivén que no aplican acá). Racha (`G._idleAutoStreak`) de auto-movimientos **consecutivos**: `onPlayerMove(x,y,isAuto)` ahora recibe un 3er parámetro — si elegís vos a tiempo (`isAuto` falsy, el click real en `game.js:919`), resetea la racha a 0; `autoMoveIdle()` se llama a sí misma con `isAuto=true` para no auto-resetearse. Se reinicia también al arrancar una partida nueva (`startOnlineGame()`).
  - **Al llegar a 3 auto-movimientos:** `forfeitByIdle()` — reusa el mecanismo de presencia YA existente (`Net.leave()` borra tu propia marca `presence/{role}`) en vez de escribir una pantalla de derrota nueva: el rival YA tiene `onOpponentLeft()` armado con su propio período de gracia para exactamente este caso (presencia ausente → victoria por abandono). Al jugador desconectado se lo manda a `home` con un toast explicativo.
  - **Fuera de alcance a propósito:** Torneo online x4 (`OT`) — nunca verificado end-to-end con dispositivos reales (ver backlog), forfeitear ahí requeriría además actualizar el bracket/eliminación, que no está resuelto. Si se pide después, revisar `Net.detachMatch()` (no borra la sala entera, a diferencia de `Net.leave()`) y cómo marcar al jugador como eliminado en `OT`.
  - Validado con Playwright (sin conexión real a Firebase — confirmado que `Net.pushMove`/`Net.leave` no-opean seguro sin `this.ref`): barra invisible→verde→amarilla→roja con anchos decrecientes, y el 3er trigger lleva correctamente a `G.online=false` + pantalla home.

## JUEGO — resumen y valores clave
Tablero 7x7 (9 paredes), movimientos simultáneos. Ítems: 🗡️ power_dmg, ◈ power_def, × down (trampa), 💍 ring (heal raro). Adyacentes/misma casilla → duelo de reflejos (frenar aguja en zona de color). Jugador abajo-derecha, rival arriba-izquierda.
- **CFG (v0.2.62+):** maxHp 100, powerDmgValue 3, powerDefValue 1, maxPowerDmg/Def 4 (techo +12 atk/+4 def), downDamage 10, regenInterval 4, counts 3/3/4. Duelo: duelCycleDuration 1.8, duelMaxPasses 4. Zonas (0..1): green .46-.54, yellow .40-.60, orange .35-.65, perfect .487-.513. Scores: perfect 20, green 10, yellow 6, orange 4, orange2 3, redBase 2, redMin 1. Ring: 6%/turno desde turno 8, +50 instant si <40 HP y 20+ abajo del rival, si no +5/ronda x5.
- **Ganador del duelo = solo puntaje crudo (0-20)**; buffs solo escalan daño (`computeDuelDamages`/`duelDamage`, 3 sitios: showDuelReveal, resolveDuel, resolveDuelOnline). `loserChipDamage` clampeado a ≤ daño ganador. PERFECTO solo pase 1 (`isPerfect`), anula ataque+defensa rival, duplica buffs propios de daño.
- **Piedad:** trampas nunca matan (floor 1 HP), chip no mata a 1 HP; solo perder un duelo mata.
- **Colisión de ítems:** sorteo determinista `(turnCount*31+x*7+y*13)%2` + ruleta visual (~1.5s). Trampa compartida golpea a ambos.
- **CPU/IA (v0.2.75, solo offline):** `cpuDuelAdvantage()` decide pelear/huir; gradiente al ítem más valioso; anti-ping-pong con fallback + detector de ciclo (bug #17 cerrado, 0 ping-pongs en 600 partidas simuladas); duelo consciente del estado. Puntería: gaussian jitter, floor 0.3, coef 0.28.
- **Veredicto de duelo:** reveal "GANA {nombre}" + puntajes chicos; pantalla final con barras HUD animadas (vacían desde HP previo, número sincronizado, colores is-low/is-mid, respeta reduced-motion, escala con maxHp real).

## MODOS
- **👤 Usuarios (v0.2.79):** identidad permanente/única/global, minúscula (`^[a-z0-9_]{3,15}$`), distinta del nickname. Reclamo transaccional en `usernames/{usuario}`→uid (no robable) + copia en `users/{uid}/username`; caché `rally_user`. Objeto `User` (load/create/set/normalize/updateUI). UI: `btn-user`, overlay de creación, visible en `home-foot`. En HUD: nickname donde siempre, usuario abajo chico/gris (`.hud-user`); rival solo online, con regla anti-suplantación. También en lobby de torneo.
- **🔑 Login/password (v0.2.80):** Firebase Auth solo maneja email+pass → email sintético `usuario@user.rallye-online.app`. Registro = reclamar usuario + `linkWithCredential` sobre cuenta anónima (mismo uid). `login()` = signIn por email/pass, cross-device. `addPassword()` = upgrade de usuarios v0.2.79 sin pass. `logout()` = signOut + limpia caché (bloqueado si es anónimo con usuario, para no perderlo). Overlay `UserUI` con 3 modos. Pass mínima 6 chars. **Sin recupero de contraseña** (avisar al usuario si se olvida). ⚠️ Requiere habilitar proveedor Email/Password en consola (pendiente).
- **Testing E2E de auth:** CDN gstatic bloqueado por el proxy — usar los bundles compat del paquete npm `firebase@10.12.2` servidos localmente. Emuladores declarados en `firebase.json` (sección `emulators`, si no el de auth no arranca). Script `e2e-auth.js` en scratchpad: 20/20. ⚠️ El emulador de **Auth** conserva usuarios entre corridas aunque se borre la RTDB — limpiar también con `curl -X DELETE http://127.0.0.1:9099/emulator/v1/projects/rallye-online/accounts` o los registros duplicados fallan con `email-already-in-use`.
- **⚙️ Panel de admin `/admin/` (v0.2.83, endurecido 2026-07-04):** página en `public/admin/index.html`, autocontenida, deploya sola → `rallyyy.web.app/admin/`. Login = usuario+contraseña de Rally. **Acceso real restringido a admins**: tras loguearse, si la cuenta NO es admin ve solo un mensaje de bloqueo (nada de lista de usuarios ni stats) + opción de auto-reclamarse SOLO si `admins/` está totalmente vacío (bootstrap; se intenta a ciegas, sin pre-leer, porque un no-admin no puede listar `admins/`). Siendo admin: (1) 👥 lista de `usernames/`, (2) ⚙️ stats editables (réplica de `LAB_PARAMS`, escriben `config/`, el juego las aplica solo al cargar vía `applyRemoteConfig()` junto a `CFG_DEFAULTS`), (3) 🔑 **sección "Administradores"**: lista quiénes son admin + input para agregar otro admin existente (busca su uid en `usernames/`, escribe `admins/{uid}=true`). Sin remoción desde la UI (solo consola).
- **Reglas `config`/`admins` (endurecidas):** `config` de lectura pública, escritura solo-admin, valida clave alfabética+valor numérico. `admins`: **listar el nodo completo requiere YA ser admin** (`.read` a nivel `admins` = `root.child('admins').child(auth.uid).val()===true`); cualquiera puede leer SU PROPIA clave (`admins/$uid` con `.read: auth.uid===$uid`, necesario para el gate sin depender de listar). Escritura: bootstrap self-claim solo si `admins/` está vacío, O un admin existente agrega cualquier otro uid — nunca se puede escribir `false`/borrar (remoción = solo consola).
- **⚠️ Trampa de diseño ya pisada:** `users/{uid}` es de lectura **solo-dueño** (privacidad, pensado para progreso futuro) — la sección de Administradores NO puede usarlo para mostrar el nombre de OTROS admins. Se resuelve leyendo `usernames/` completo (sí es legible por cualquier autenticado) e invirtiéndolo en el cliente (nombre→uid pasa a uid→nombre). Si se agrega cualquier otra vista que muestre "de quién es este uid", usar el mismo patrón — nunca `users/{uid}/...` de un uid ajeno.
- **Otorgar el primer admin sin acceso a la RTDB de prod:** `.github/workflows/grant-admin.yml` (workflow_dispatch, input `username`) usa el Admin SDK de Firebase + el secret `FIREBASE_SERVICE_ACCOUNT_RALLYE_ONLINE` (bypassea las reglas) para poner `admins/{uid}=true` a partir de un nombre de usuario. Sirve tanto para el bootstrap inicial como para recuperar acceso si algo se traba. Se dispara desde la pestaña Actions de GitHub (o `actions_run_trigger` vía MCP).
- **Mantener sincronizados:** la lista `PARAMS` en `public/admin/index.html` debe reflejar siempre `LAB_PARAMS` de `game.js` (mismas claves y defaults) — si se agrega/cambia un parámetro de balance en uno, replicarlo en el otro.
- **📝 Textos del juego editables (v0.2.97, pedido de Lucio):** mismo patrón que `CFG`/`config/` pero para strings. `TEXTS` (game.js, 163 claves, justo después de `CFG`) tiene TODOS los mensajes no-botón: desconexión/inactividad, howto (mini+largo), toasts, errores de cuenta, mensajes de turno, nombres de personajes (roster/CPU/campaña), veredicto/resultado de duelo, fin de partida (1v1/bo5/campaña/torneo), y torneo online x4 (hub+espectador). **Fuera de alcance a propósito** (decisión confirmada con Lucio): labels de botones estáticos (~60-70 strings en HTML/JS, ej. "Entrar"/"Conectando…"/"Crear cuenta") y los toasts del panel Lab (dev-only).
  - **Placeholders `{llave}`:** strings con valores dinámicos embebidos (nombre, HP, score, HTML de color) usan tokens `{nombre}`/`{hp}`/etc en vez de template literals — `fillText(key, vars)` (junto a `TEXTS`) hace el reemplazo (`TEXTS[key].replace(/\{k\}/g, vars[k])` por cada var). Ej: `fillText('tourneyBeatOpp', {name:r.name})`. Las que no llevan vars van directo por `TEXTS.clave`.
  - **`applyRemoteTexts()`** (game.js, junto a `applyRemoteConfig()`): aplica defaults sincrónico vía `applyTextsToDom()` (llena `#howto-*`/`#info-*` que en `index.html` son ahora elementos VACÍOS con id, ya no prosa estática — parcheo lo mínimo indispensable, headings/íconos siguen fijos), después pide `texts/` a Firebase y pisa `TEXTS[k]` solo si la clave YA existe como string en el objeto default (`typeof TEXTS[k]==='string'`, blindaje contra basura) — si hubo cambios, vuelve a llamar `applyTextsToDom()`.
  - **Trampa de orden ya evitada:** `TOURNEY_ROSTER`/`CAMPAIGN_SCRIPT` están declarados ANTES que `TEXTS` en el archivo (no se movieron, para no arriesgar romper código intermedio) — no se puede leer `TEXTS.rosterNameN` en su declaración (TDZ). Se resuelve parcheando `.name` de cada uno DENTRO de `applyTextsToDom()` (que corre después, sync + tras el fetch), mismo con `CPU_NAMES` (se vacía y rellena in-place con `.length=0`+`.push(...)`, porque es `const`).
  - **Panel de admin:** nueva sección "📝 Textos del juego" (misma UI que Stats pero con `<textarea>` en vez de `<input type=number>`) — `TEXT_PARAMS` (public/admin/index.html, **mantener sincronizada con `TEXTS` de game.js**, mismo trato que `PARAMS`/`LAB_PARAMS`) es un array `[clave, grupo, default]` generado 1:1 desde `TEXTS` (163 entradas, agrupadas por las mismas categorías que los comentarios de `TEXTS`). El label de cada campo es la clave "humanizada" (`humanizeKey()`, camelCase→palabras separadas) en vez de un label a mano por entrada — con 163 strings no valía la pena escribir 163 labels custom, y el valor del campo YA es autoexplicativo (es el texto real). `loadTexts()`/`tx-save`/`tx-reset` calcan `loadStats()`/`st-save`/`st-reset` (mismo patrón: default = borrar override, override = escribir; `.is-changed` si difiere del default).
  - **Reglas (`database.rules.json`):** nodo `texts` gemelo de `config` — lectura pública, escritura solo-admin, `.validate` por clave: alfanumérica ≤40 chars, `isString()`, tope de 2000 chars (evita que un admin cargue algo gigante por error).
  - Validado con Playwright (sin Firebase real — stub del SDK vía `page.route` para el admin, e inyección directa de `TEXTS`/`applyTextsToDom()` para el juego): defaults se renderizan en howto/info/duelo/resultado; override de `TEXTS` en runtime se refleja en DOM + `TOURNEY_ROSTER`/`CPU_NAMES`/`App.oppName`; admin renderiza las 163 textareas agrupadas, marca `is-changed` con un override simulado, guarda y restaura defaults correctamente.
- **📊 Estadísticas de jugador (v0.2.84):** 6 contadores online-only en `users/{uid}/stats/{gamesPlayed,gamesWon,kills,tournamentsWon,damageDealt,damageReceived}` — NO cuenta práctica offline ni torneo offline (Tourney), ni partidas de torneo x4 rellenadas con CPU. Objeto `Stats` (junto a `User`, game.js) con `bump(uid,key,delta)` (transaction, atómico) y `bumpMany(uid,deltas)` (omite deltas en 0 — no escribe no-ops). 4 puntos de enganche:
  - **Daño + kills** en `resolveDuelOnline()`: justo después de aplicar HP en cada rama (gana/pierde), usa las mismas variables ya calculadas (`yourRealDmg`/`chip`/`oppRealDmg`); `kills` solo si `G.opp.hp<=0` tras tu victoria (no todo duelo ganado es un kill — hay que bajar al rival a 0).
  - **gamesPlayed/gamesWon** en `endGame()` bloque `if(G.online)`, dentro de `if(matchOver)` (cierre de la serie, no de cada ronda de un bo5).
  - **gamesPlayed/gamesWon** en `OT.onMyMatchEnd()` (partida de torneo x4), gateado por `wasOnline` (capturado ANTES de que la función resetee `G.online=false` — si el rival era CPU de relleno, `G.online` ya era `false` y no cuenta).
  - **tournamentsWon** en `OT.renderHub()` cuando `fw===this.mySeat` (soy el campeón), con guarda `OT._champBumped` reseteada en `resetRunFlags()` y `route()` para no sumar de nuevo en re-renders del hub.
  - Reglas: `users/$uid` ahora permite lectura a admins (antes solo dueño) + `.validate` en `stats/$statKey` (clave de una whitelist fija, número ≥0). **Validado: 100/100 tests de reglas + 19/19 E2E** simulando duelos/partidas/torneo con estado controlado vía `page.evaluate` (más práctico que jugar la UI real) — script `e2e-stats.js` en scratchpad.
  - **UI jugador:** overlay de cuenta (`UserUI`, modo `session`) ahora muestra Partidas ganadas / Kills / Torneos ganados (`loadProfileStats()`, lee su propio nodo) + línea "🏆 Logros: Todavía no" (gris, placeholder sin lógica todavía).
  - **UI admin:** nueva tabla "📊 Estadísticas de jugadores" en `/admin/` (bajo la de usuarios) con las 6 stats + columna Logros ("Todavía no" fijo); tabla con scroll horizontal en mobile (`.table-scroll`). `loadPlayerStats()` hace un `usernames.get()` + un `users/{uid}/stats.get()` por usuario.
  - **Límite conocido, aceptado:** sin anti-cheat — cualquiera con la consola del navegador podría escribirse sus propias stats directo (mismo modelo de confianza que moves/duelos ya existentes; las reglas solo validan forma, no procedencia).
  - **Bug "no se guardan las stats" (reporte de Lucio 4/7, fix v0.2.88):** dos fallas silenciosas, ninguna daba error visible. (1) `Stats.bump()` tragaba cualquier error real de Firebase con `.catch(()=>{})` vacío — ahora loguea `console.error`. (2) los 4 puntos que llaman a `Stats.*` leían `firebase.auth().currentUser` de forma SÍNCRONA (`User.current()`); si la sesión anónima todavía se estaba restaurando en ese instante, `currentUser` daba `null` y el guardado se saltaba entero, sin error. Fix: los 4 (`resolveDuelOnline`, `endGame`, `OT.onMyMatchEnd`, `OT.renderHub`) ahora usan `ensureAuth().then(u=>{ if(u) Stats.bumpMany(...) })`, mismo patrón ya probado en `trackVisit()`.
- **📈 Visitas del sitio (v0.2.85):** distinto de Stats — cuenta CADA carga de `index.html` (juegue o no online), en `siteStats/pageViews` (contador global) y `siteStats/visitors/{uid}` (set de uids → el conteo de únicos es simplemente la cantidad de claves). `trackVisit()` (game.js, junto a `Stats`) se llama una sola vez al cargar el script — dispara `ensureAuth()` de una, ANTES de que el jugador intente ir online (antes la auth anónima era perezosa, solo al crear/unirse a sala). Esto es seguro gracias al fix #23 (`ensureAuth` ya espera el primer `onAuthStateChanged`), así que nunca pisa una sesión con contraseña. "Visitante único" = mismo uid persistente (mismo navegador/dispositivo); recargar la página NO duplica el único, pero sí suma a `pageViews`. Panel de admin: sección "📈 Visitas" (arriba de todo), muestra ambos números, `loadSiteStats()`. **Solo cuenta visitas al juego, NO a `/admin/`** (ese archivo no tiene `trackVisit()`).
  - **⚠️ Trampa de rules ya pisada:** `.transaction()` de Firebase necesita permiso de LECTURA además de escritura (lee el valor actual antes de calcular el nuevo) — `siteStats/pageViews` tiene `.read: auth != null` propio (a nivel de esa clave puntual, no del nodo padre) aunque `siteStats` en general sea de lectura solo-admin; si no, la transacción de cualquier visitante no-admin falla en silencio (catch vacío) y el contador nunca sube. `visitors/{uid}` no tuvo este problema porque usa `.set()` plano, no `.transaction()`. **Si se agrega otro contador global con `.transaction()`, agregar su `.read` puntual explícito** o va a fallar igual de silencioso.
  - Validado: 111/111 tests de reglas + 9/9 E2E (dos "visitantes" reales cargando la página de verdad — no simulado — con reload persistiendo el mismo uid, contra un tercer navegador con uid distinto).
- **Campaña offline (v0.2.63+):** `CAMPAIGN_SCRIPT` = nodos (`match` con `opp:{name,hp,skill,accent,emoji,dmgMult}`+`youHp`; `scene` vía `playScene()`; extensible con `Campaign.handlers`). Progreso en localStorage `rally_campaign_v1`, guarda al ganar cada nodo. `Campaign.setFlag/getFlag`. Primer rival: Tarata, 11 HP, skill 0.35. Arranca sin fade/overlay. Ganar→"Continuar"; perder/empatar→"Reintentar". Salir no borra save. Skill CPU: campaña > torneo > 0.35. Concepto: parece partida normal, van apareciendo mecánicas/historia — el usuario la va escribiendo.
- **Torneo offline:** 8 rivales TOURNEY_ROSTER (Madagascar) con skills especiales (Marlene doubleStep, Alex hardHit, Rey Julian luck). HP 10→200 y skill 0→1 exponenciales. HP se conserva entre rondas. Bracket estilo Mortal Kombat.
- **Torneo online x4 (`OT`, v0.2.60):** sistema separado — salas, hub, bracket, ruteo, espectador. ⚠️ Nunca verificado end-to-end con dispositivos reales. Post-partida: "volver a la sala" (reemplazó el rematch viejo).
  - **Espectador ve el duelo, no solo el resultado (v0.2.86, reporte de Lucio 4/7):** antes `OT.pushSpec()` solo se llamaba en los bordes de vida del match (arranque, fin de turno, fin de match) — nunca durante el duelo en sí, así que el espectador solo veía "Turno N" y de golpe el próximo turno, sin enterarse de que hubo un duelo. Fix liviano (sin sincronizar la aguja en vivo): `pushSpec(duelInfo)` ahora acepta un campo opcional `duel` — se llama con `{active:true}` al arrancar el duelo (`startDuelOnline()`) y con `{active:false, winner:'A'|'B'|'tie', scoreA, scoreB}` al resolverse (`resolveDuelOnline()`, el master calcula el ganador desde su propia perspectiva de seat). `renderSpec()` muestra "⚔️ ¡Duelo en curso!" o "⚔️ Ganó {nombre} (score-score)" / "🤝 Empate (score-score)"; el aviso desaparece solo en el próximo `pushSpec()` normal (sin `duel`) de `startChoosePhase()`, porque es un `.set()` completo. Reglas: agregado `spec/duel` (whitelist `active`/`winner`/`scoreA`/`scoreB`, mismo patrón `$other:false`). **Validado: 118/118 tests de reglas + 5/5 E2E** (dos pestañas reales — una "master" publicando, otra "espectador" con `OT.spectate()` — verificando el texto en pantalla en cada etapa: turno→duelo en curso→resultado con score→vuelta a turno→empate). Pendiente si lo pide a futuro: sincronizar la aguja del duelo en vivo (más laborioso, requiere pushear posición de la aguja en tiempo real).
- **Modo Paredes (beta):** toggle en lobby (solo host, partida única/bo5, no torneo x4). Serialización online con prefijo "W". Falta test online real.
- **Modo oscuro (hecho ~v0.2.76-77):** toggle `btn-theme`, `data-theme`, localStorage `rally_theme`, claro por defecto. Anti-FOUC con script inline en `<head>`.
- **Chat online (v0.2.62):** panel plegable + badge no leídos, objeto `Chat`. Cubre 2p y torneo. Confirmado en producción.
- **🧪 Laboratorio:** `?lab=1` o 5 taps en version-tag. Sliders sobre CFG, export/import JSON, reset, spawnear anillo. Seguro de shippear. Incluye checkbox "Forzar PERFECTO siempre" (testing) que fuerza tu frenada a la banda de PERFECTO (`labForcePerfect`, offline y online).
- **Pantalla "Cómo se juega" (`#howto`, v0.2.90-91):** rediseño pedido por Lucio con mockup de referencia — párrafo acortado, mismo legend/hint, y **nueva franja mini de speedómetro decorativa** (`.howto-speedo`, reutiliza las clases `.speedo-zone.is-*` reales) con una aguja que barre sola en loop continuo vía CSS (`@keyframes howtoNeedleSweep`, 0.9s por tramo = mismo ritmo que `CFG.duelCycleDuration`). Es puramente ilustrativo, no hay duelo real corriendo detrás. Sin cambios en el trigger (`beginGame._seen`, una vez por sesión, Campaña/Torneo la siguen salteando).
  - **⚠️ Trampa de animación ya pisada (v0.2.91):** la primera versión animaba `left` en vez de `transform` → se veía trabada (layout en cada frame, no compositado por GPU). Fix: la aguja visible va dentro de un wrapper (`.howto-speedo__sweep`) que ocupa el 100% de la franja, y se anima `transform:translateX(0 → calc(100% - 3px))` sobre ESE wrapper — el `%` de `translateX` es relativo al ancho propio del elemento, así que da el mismo barrido de punta a punta sin fijar píxeles (sigue fluido/responsive) y sin disparar reflow. **Si se anima cualquier otra posición en franjas/barras decorativas, usar este mismo patrón (wrapper 100% + transform), nunca animar `left`/`top` directo.**
- **Bug de UX — nombre de jugador sin límite (fix v0.2.91):** `#name-input`/`#join-name` no tenían `maxlength`, pero `host.name`/`guest.name`/`players.$seat.name`/chat `name` en `database.rules.json` exigen `length<=40` — un nombre más largo rompía la escritura entera (rechazada por las reglas) con un toast engañoso ("Revisá tu conexión"). Fix: `maxlength="40"` en ambos inputs.
- **Globos "Vos"/rival al arrancar la partida (v0.2.92):** `showStartBubbles()`/`spawnStartBubble()` (game.js, junto a `startGame`) — burbuja azul "Vos" sobre tu ficha y burbuja negra con el nombre del rival sobre la suya, aparecen una vez al arrancar cada partida (enganchado al final de `startGame()`/`startOnlineGame()`, DESPUÉS de su primer `renderBoard()`+`startChoosePhase()` — nunca dentro de `renderBoard()` mismo, que se llama en cada movimiento) y se desvanecen solas ~1.35s después (clase `is-show`→`is-gone`, se remueven del DOM). Posición calculada una sola vez con `getBoundingClientRect()` sobre `.player-marker.is-you`/`.is-opp` (a prueba de flip del guest) contra `.board-wrap` (ojo: es una CLASE, no tiene `id` — hay que usar `document.querySelector`, `$()` es `getElementById` y devuelve `null` en silencio).
  - **Texto del rival según modo** (confirmado con Lucio): "Rival" genérico en práctica rápida/demo y Campaña; nombre real (`App.oppName`) en Torneo offline, online 1v1 y Torneo online x4 (humano o CPU de relleno) — condición: `Campaign.active || (!G.online && !Tourney.active && !OT.active)` → generic. Validado en los 3 casos representativos con Playwright (Cachito→"Rival", roster de Torneo→nombre real, Campaña→"Rival").
  - CSS nuevo `.start-bubble*` (junto a `.howto-speedo`) — reutiliza `--accent`/`--ink` (mismos colores que `.player-marker.is-you/.is-opp`), con una colita triangular vía `::after`. Sin escritura nueva a Firebase (puramente visual/local).
- **Animación de movimiento entre casillas, incluye diagonales (v0.2.93-94):** `renderBoard()` destruye y reconstruye TODO el tablero en cada llamada (sin ficha persistente que animar con left/top directo), así que el desplazamiento fluido se hace con la técnica **FLIP** — `getMarkerRect(cls)`/`flipMarker(cls, oldRect)` (game.js, junto a `resolveMoves()`): medir el rect de `.player-marker.is-you`/`.is-opp` ANTES de aplicar las coordenadas nuevas y llamar a `renderBoard()`, y después invertir la diferencia con `transform:translate()` sin transición, soltándolo un frame después (doble `requestAnimationFrame`). Sirve igual para ortogonal o diagonal (delta 2D genérico, sin casos especiales por dirección). Enganchado en el ÚNICO lugar donde se aplican coordenadas nuevas (`resolveMoves()`, compartido por offline y online — `onPlayerMove()`/`onOnlineMovesReady()` convergen ahí), así que cubre ambos modos con un solo cambio.
  - **Curva propia, no la de `.player-marker` (fix v0.2.94):** la primera versión soltaba el transform sin `transition` inline, heredando la de `.player-marker` en CSS (`cubic-bezier(.34,1.56,.64,1)`, "back-out" — pensada para el pop de choque `.is-clash`) y el deslizamiento se veía con rebote/overshoot. Pedido de Lucio: "carga y llegada", sin rebote. Fix: `flipMarker()` ahora fija su PROPIA `transition: transform .35s ease-in-out` al soltar el transform (no toca la regla CSS compartida, así el pop de choque sigue con su bounce intacto), y la limpia con `transitionend` al terminar. Validado con muestras de `getBoundingClientRect()` cada 35ms: perfil lento-rápido-lento simétrico, sin overshoot en ningún eje.
  - **Caso excluido a propósito:** si ambos movimientos coinciden en la misma casilla (choque, `.is-clash`), no se anima con FLIP — ese caso ya tiene su propio transform fijo de separación visual y combinarlo con el desplazamiento arriesgaba romperlo. El reposicionamiento post-empate (`ejectPlayers()`) tampoco se tocó (fuera de alcance, es otro flujo).
- **Bug "aguja del duelo trabada" (fix v0.2.95):** `updateNeedles()` (game.js) escribía `style.left` en CADA frame del duelo (~60/seg, rAF loop de `beginDuelPlay`/`beginDuelPlayOnline`) sobre `#speedo-needle`/`#speedo-needle-opponent` — mismo bug de layout-thrashing que ya se había arreglado en la franja mini del howto (v0.2.91), pero nunca se replicó acá porque son elementos totalmente distintos (no comparten clase). Fix: `buildSpeedometer()` cachea `G.duel.trackWidth` UNA vez por duelo (`getBoundingClientRect().width` del track), y el nuevo helper `setNeedleX(el,pos,trackW)` mueve la aguja con `transform: translateX(pos*trackW + 'px') translateX(-50%)` (dos translateX encadenados: uno posiciona en píxeles, el otro centra la aguja de 4px sobre ese punto) en vez de `left`. CSS: `will-change: left` → `will-change: transform` en ambas agujas (ahora sí es la propiedad realmente animada). **Si se agrega OTRA barra/aguja que se mueva en vivo, aplicar el mismo patrón desde el principio** (no solo en la mini decorativa) — cachear el ancho del track una vez y mover con transform, nunca escribir `left`/`top` en un loop.
- Extras: easter egg "messi", nombre cacheado `rally_name`, invitación `?sala=CODE`, limpieza de salas >2hs, indicador "el rival ya eligió".
- **Optimización — reducir duplicación/tamaño (v0.2.98, pedido de Lucio "Optimiza el codigo"):** sin cambios de comportamiento, solo refactor mecánico validado con Playwright antes/después (mismos resultados). Puntos:
  - **Duelo offline/online:** `startDuel()`/`startDuelOnline()` y `beginDuelPlay()`/`beginDuelPlayOnline()` (game.js, cerca de línea 1938) tenían el DOM/countdown/reseteo de estado casi idéntico — extraído a `showDuelCountdownUI()`+`runDuelCountdown(onDone)` y `resetDuelPlayState()`+`startDuelRaf(updateFn)`; cada variante offline/online solo aporta lo que realmente difiere (CPU timer, listeners de Firebase).
  - **Resolución del duelo:** `resolveDuel()`/`finishDuelOnline()` compartían el cálculo de daño/HP/showDuelOutcome — extraído a `applyDuelOutcome()` (devuelve `{isTie,youWin,rawYou,rawOpp,yourRealDmg,oppRealDmg,chip}`); cada caller hace su propio post-proceso (offline: eject simple; online: stats + sync de eject host/guest + spec de OT).
  - **`escapeHtml`/`escHtml`:** dos implementaciones idénticas de escape de HTML — quedó solo `escHtml` (más usada, 9 call sites vs 2), sin cambio de comportamiento.
  - **Ícono de ítem de celda:** `renderBoard()` y `OT.renderSpec()` (espectador de torneo online) repetían el mismo if/else de 4 líneas (tipo→ícono) — extraído a `appendCellItemIcon(div, type)`.
  - **Color de barra de HP:** `updateHud()` y el closure `col()`/`setColor` de `showDuelOutcome()` repetían los mismos umbrales `<25`/`25-55` — extraído a `setHpBarColor(fillEl, pct)`.
  - **Código muerto eliminado** (confirmado sin ninguna otra referencia): `Net.pushRematch`/`Net.listenRematch`/`Net.stopListenRematch`/`Net.onRematchState` (revancha online vieja, nunca conectada — el flujo real usa `resetForRematch()` solamente) y `Net.stopListenBoard`.
  - **Firebase — un viaje de red menos:** `OT.route()` hacía DOS `.get()` secuenciales (`players`, después `bracket/r1`) sobre la misma sala — ahora un solo `this.ref.get()` y ambos se leen del mismo snapshot vía `.child(path)`.
  - **Evaluado y descartado por ahora** (mayor riesgo/beneficio marginal): fusionar los nodos `config`/`texts` de Firebase en un solo read al cargar la página (ahorraría 1 round-trip más mas requeriría migrar el schema en producción); no tocado. `OT` sigue "nunca verificado end-to-end con dispositivos reales" — el fix de `route()` se validó por lectura de código + que la API `snapshot.child(path)` es estándar del SDK compat, no con un dispositivo real.
  - Validado con Playwright: duelo offline completo (countdown→play→reveal→resultado, con `labForcePerfect`), duelo online simulado (mismo flujo con `Net` sin conexión real), conteo de íconos en el tablero, partida completa hasta pantalla de resultado — mismos resultados que antes del refactor.
- **Panel de admin trabado en "Entrando…" (reportado por Lucio, se resolvió solo):** el usuario reportó no poder entrar al panel; investigando, el código de login (`admin/index.html`, `$('l-go')` click handler) no tenía ningún bug evidente — `signInWithEmailAndPassword`/las lecturas de `db.ref(...)` están bien encadenadas y el botón se re-habilita en el `finally` implícito tras el try/catch. El síntoma ("Entrando…" indefinido sin error) apunta a que la promesa de Firebase (Auth o RTDB) nunca resuelve NI rechaza — típico de un problema de RED/conectividad (bloqueo de `*.firebaseio.com`/`identitytoolkit.googleapis.com` por firewall, extensión del navegador, o conexión inestable), no de código. Se resolvió sin cambios de código (probablemente transitorio). **Si vuelve a pasar:** pedir que abran la consola del navegador (F12) durante el intento, y confirmar si el mismo usuario/contraseña funciona en el LOGIN DEL JUEGO (no admin) — si también se traba ahí, confirma que es de conexión/Firebase Auth y no del panel.
- **Texto de "Cómo se juega" simplificado + footer de crédito (v0.2.99, pedido de Lucio):** los párrafos largos de `TEXTS.infoIntro`/`infoItemDmg`/`infoItemDef`/`infoItemTrap`/`infoItemRing`/`infoDuelIntro`/`infoPerfect`/`infoPerfectCancels`/`infoScoreDecay`/`infoMercy`/`infoWalls` (game.js) se acortaron a la mitad aprox., mismo significado — actualizado también en el mirror `TEXT_PARAMS` de `admin/index.html` (¡recordar mantenerlos sincronizados si se vuelven a editar desde cualquiera de los dos lados!). Nuevo `<p class="page-credit">Sin © 2026<br>Creado por Lucio</p>` al pie de `#screen-home` (index.html), estilo sutil/opacado (`.page-credit`, style.css) — texto literal confirmado con el usuario (no es un typo).

## LECCIONES (leer antes de tocar código)
- **`[hidden]` vs CSS:** si un contenedor con `hidden` tiene `display:` en CSS de autor, agregar SIEMPRE `[hidden]{display:none}` explícito.
- **Flex centrado + contenido variable:** `margin:auto` en hijos + `overflow-y:auto`, no `justify-content:center` (recorta contenido — bug "Salir cortado").
- **Rings/bordes en móvil:** `box-shadow: inset`, no hacia afuera (se recorta).
- **Verificar merge siempre:** después de mergear, chequear `git log origin/main` + diff contra la rama — no asumir que tomó todo (incidente PR #1: merge parcial + commit "Revert" espurio, se reconstruyó con cherry-pick).
- **Cache HTML/JS:** de ahí el `?v=` por versión — mantenerlo al bumpear.
- Versionado: el usuario copia y renombra la carpeta de versión — es por carpeta, sin importar la herramienta.

## BACKLOG (repriorizado 2026-07-04)
- **CRÍTICO — resuelto en esta sesión:** publicar `database.rules.json` ya no depende de la consola — deploy automático desde v0.2.89 (ver DEPLOY/FIREBASE). Este merge es la primera publicación real. **Pendiente del usuario:** habilitar Email/Password en Authentication (login con contraseña no funciona hasta entonces). Auth anónima ✓, deploy automático (hosting + reglas) ✓, admin de "lucio" ✓ confirmado en producción.
- **Endurecer validación de campos compuestos en `database.rules.json`:** `moves`, `ejects`, `spec.A/B` no fuerzan que el valor sea un objeto (`hasChildren`) — ver nota en FIREBASE. Bajo impacto, no urgente.
- **📈 Visitas del sitio:** hecho en v0.2.85 (ver MODOS/FIREBASE). A futuro si lo pide: distinguir visitas nuevas vs. recurrentes, visitas por día/semana (hoy es un total acumulado sin fecha).
- **Estadísticas de jugador:** hecho en v0.2.84 (ver MODOS/FIREBASE). A futuro si lo pide: sistema real de logros (hoy es un placeholder gris "Todavía no" en perfil y admin, sin lógica), mostrar daño hecho/recibido también en el perfil del jugador (hoy solo en admin), ranking/leaderboard entre jugadores.
- **Login real:** hecho en v0.2.79-80. A futuro: migrar progreso (campaña, etc.) de localStorage a `users/$uid`; eventualmente email real/Google para recupero de contraseña.
- **Panel de admin `/admin/`:** hecho y endurecido (solo-admins entran, sección para agregar admins, ver MODOS/FIREBASE). **"lucio" YA es admin en producción** (confirmado: `grant-admin.yml` run exitoso 2026-07-04, `usernames/lucio → admins/{uid}=true` vía Admin SDK). ⚠️ El primer disparo del workflow quedó colgado ("in progress" varios minutos) por un bug real: el Admin SDK deja abierta la conexión de la RTDB y Node no cierra solo — se corrigió agregando `process.exit(0)` tras el éxito; si se edita este workflow de nuevo, no perder ese detalle. A futuro si lo pide: dominio/site propio (hoy vive en `rallyyy.web.app/admin/`), remoción de admins desde la UI, historial de cambios de config.
- **Testear en vivo (2 dispositivos):** torneo online x4 (OT) end-to-end y Modo Paredes online.
- **#19 Torneo:** curación extra entre rondas, animación al subir puesto, rivales ocultos hasta desbloquear.
- **#18 Modo Veneno:** no arrancado.
- **Campaña:** escribir nodos nuevos (infraestructura lista).
- Hechos y verificados: #23 se cerraba el login solo (v0.2.82, reporte de Lucio 3/7), #22 "sala llena" fantasma al unirse (v0.2.81, reporte de Lucio 3/7), #17 vaivén IA, #21 modo oscuro, #10 perfecto anula buffs rivales, #20 perfecto solo pase 1, #18b Paredes, dark-mode FOUC, #24 stats no se guardaban (v0.2.88), #25 reglas de `ejects` rechazaban la forma real + suplantación en `players.user` de torneo + revelado de duelo cortado al frenar segundo (v0.2.89).

## PREFERENCIAS DEL USUARIO
- Visuales mínimos e integrados (bajar efectos llamativos).
- Testea offline entre cambios; deploya vía merge automático (ver reglas fijas arriba). Cuida tokens: incremental, un set de cambios por vez, validar antes de entregar.
- Le gusta que le ofrezcan opciones en decisiones de diseño/mecánica, y herramientas de testeo (Lab, spawnear anillo).
- Suele mandar varios pedidos juntos; hacerlos "de una" si son chicos, validando igual incrementalmente. Mantener y repriorizar este backlog.

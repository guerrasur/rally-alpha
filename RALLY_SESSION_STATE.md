# Rally — Session State & Learnings

**Última actualización:** v0.3.30 — 2026-07-12, sesión remota (Claude Code), caza de bugs sobre el import de v0.3.29.

## CHANGELOG (resumido, más reciente primero)
- **v0.3.30 — caza de bugs sobre v0.3.29 (2 fixes, VERIFICADOS en browser real):** (1) **"↺ Retomar torneo" nunca aparecía al cargar la página** — `updateTourneyResumeUI()` corría en el setup ANTES que `TourneyProgress.load()` (~70 líneas más abajo), y `load()` no refrescaba la UI: con progreso guardado en localStorage el botón quedaba oculto hasta registrar progreso nuevo en la misma sesión o abrir el perfil logueado (la feature quedaba rota justo en su caso principal: volver otro día). Fix: `load()` termina llamando `updateTourneyResumeUI()` (mismo patrón que `record()`/`loadRemote()`); quitada la llamada suelta redundante. (2) **El popup "+N HP" reaparecía y quedaba pegado en pantalla** — los keyframes de `healPop` terminan en `opacity:0` pero la animación no tenía `animation-fill-mode:forwards`: al cumplirse los 1.1s el elemento volvía a sus estilos base (opacidad 1) y el texto quedaba fijo; encima las ramas online/campaña de `endGame()` no llamaban `showHealPop(0)`, así que ese "+N HP" viejo se arrastraba a resultados de otros modos. Fix: `forwards` en la animación + limpieza centralizada al tope de `endGame()` (junto a las de `tourney-progress`/`round-dots`; quitadas las 3 llamadas por rama). **Verificación:** este entorno remoto SÍ tiene Chromium (`/opt/pw-browsers/`) — harness con servidor local: el baseline v0.3.29 reproduce el bug (botón `display:none` con progreso guardado); con el fix el botón aparece al cargar, el overlay lista los rivales con su HP, retomar en el rival 3 arranca el bracket con ese HP (31), el heal-pop termina en opacity 0 sin reaparecer, y sin progreso el botón queda oculto. Cero errores de consola. Con esto "Retomar torneo" queda confirmado visualmente (pendiente de v0.3.29); el selector de niveles de campaña sigue sin test visual (necesita ≥2 nodos match → solo con cinta remota de Firebase, inaccesible desde acá).
- **v0.3.29 — import desde Claude Design (proyecto "Session markdown"):** Lucio trabajó una sesión en claude.ai/design sobre los 3 archivos de `public/`; import trajo funcionalidad nueva de Torneo/Campaña. **La rama de trabajo estaba desactualizada** (basada en v0.3.26, previa a los merges de v0.3.27 y v0.3.28-Tarata que ya estaban en `main`) — pisar los archivos completos del snapshot de Design habría revertido esos dos merges. Se descartó ese enfoque: se rebaseó sobre `main` actual y se aplicó SOLO el delta real de Design (verificado con diff de 3 vías: `main` vs snapshot de Design) encima del código ya mergeado. **Nuevo:** (1) **Retomar torneo offline** — `TourneyProgress` guarda (local + cuenta, `users/{uid}/tourneyProgress`) el mejor HP con el que se llegó a cada rival del torneo; botón "↺ Retomar torneo" en el menú offline abre lista de rivales ya alcanzados, entra directo con ese HP. (2) **Curación entre rondas de torneo** — al ganar un duelo se recupera 30% (redondeado) del maxHp del rival derrotado, con popup "+N HP" (`heal-pop`, animación de un solo disparo). (3) **Selector de niveles de campaña** — si ya se superó más de un nivel, "Campaña" abre una grilla de hexágonos numerados para elegir por cuál seguir/rejugar (`Campaign.replaying` evita pisar el progreso real al rejugar uno viejo). **`database.rules.json`:** validación nueva de `users/$uid/tourneyProgress/$idx` (número 0-1000, clave numérica corta 1-2 dígitos); el `.indexOn:["createdAt"]` que la nueva query necesitaría ya existía desde v0.3.27. **Bug encontrado en el HTML importado (artefacto del editor visual de Design, no del código):** `#screen-result` traía `style="left:2px; top:0; position:absolute"` y `#tourney-progress` `style="width:390px; height:69px"` inline — el ancho fijo rompía celulares angostos (<390px, ej. iPhone SE 375px) con overflow horizontal. Removidos ambos; quedan gobernados por las clases CSS normales (ya responsive). Validado: `node -e "new Function(...)"` sobre game.js OK; JSON de reglas válido; diff final contra el snapshot de Design confirma que el único delta restante es el número de versión (que Design desconocía) — nada del trabajo de Design se perdió y nada de lo ya mergeado en `main` (v0.3.27, v0.3.28-Tarata) se pisó. **No probado en browser real** (sesión sin Chromium en este entorno) — pendiente que Lucio confirme visualmente "Retomar torneo" y el selector de niveles.
- **LECCIÓN nueva:** si el usuario trae un import de Design y hace un rato que no se toca el repo, SIEMPRE `git fetch` + comparar `main` actual contra la rama de trabajo ANTES de aplicar nada — la rama pudo quedar atrás de merges ya hechos por otra sesión, y pisar archivos enteros del import revertiría ese trabajo en silencio. Reconstruir aplicando el delta real (diff de 3 vías) en vez de sobreescribir.
- **v0.3.28 — fix: el sprite de movimiento de Tarata no aparecía en su PRIMER movimiento:** `tarata_move.webp` no se precargaba nunca — su URL se pinta por primera vez cuando `flipMarker()` setea `--sprite-url` al arrancar el primer deslizamiento, así que el browser disparaba la descarga recién en ese instante y el trayecto (0.35s) terminaba (revirtiendo al idle en `transitionend`) antes de que la imagen llegara. Del segundo movimiento en adelante ya estaba en caché HTTP → andaba. Fix: `applyOppCosmetic()` precarga la variante (`new Image().src`) al setear `G.spriteOppMove` (corre en `startGame()`, varios segundos antes del primer FLIP). Verificado con Chrome headless + servidor local (harness que loguea requests de `sprites/` entrando a la campaña real): baseline pide solo `tarata.webp` al arrancar la partida (bug reproducido); con el fix pide ambos ANTES de cualquier movimiento.
- **v0.3.27 — caza de bugs + optimización:** (1) **XSS en el veredicto del duelo** — `showDuelReveal()` metía `App.playerName`/`App.oppName` SIN escapar en `innerHTML`; online el nick del rival es input remoto (inyección HTML real). Fix: `escHtml()` en ambos. (2) **`fillText()` rompía con `$` en los valores** — `replace(RegExp, string)` interpreta `$&`/`$'` (un nombre con `$&` re-insertaba el placeholder). Fix: split/join, sin regex. (3) **Listeners duplicados de `game/board`** — `Net.listenBoard()` se llama una vez POR RONDA de la serie sin soltar el anterior: en la ronda N el guest acumulaba N listeners (N renders por update). Fix: patrón `_boardCb` idempotente, igual que `listenStart`. (4) **Duelo fantasma tras abandono** — si el rival abandonaba durante el revelado/resultado del duelo, los `setTimeout` de `finishDuelOnline`/`resolveDuel` seguían vivos: pisaban la pantalla de victoria por abandono, aplicaban daño y sumaban stats. Fix: guard `!G.running` en `resolveDuel`/`resolveDuelOnline`/`finishDuelOnline` y en sus timeouts de continuación. (5) `cpuTimer` pendiente ahora se limpia en `resetDuelPlayState()` (camino compartido, no solo offline). (6) Espectador: `escHtml`+`textContent` mostraba `&lt;` literal (doble escape) — quitado el escape. (7) **`cleanStaleRooms()` bajaba `rooms/` ENTERO** (todas las salas activas con games y chats) para filtrar en el cliente → query `orderByChild('createdAt').endAt(cutoff)` + `.indexOn: createdAt` en `database.rules.json` (aditivo, no toca permisos). (8) **Admin re-sincronizado:** faltaban 18 claves de TEXTS en `TEXT_PARAMS` (las de v0.3.16: `btnCampaign`, `userTitle*`, `homeFoot*`, etc.) — verificado por script: 193/193 claves y defaults idénticos. (9) Micro: Set O(1) para `is-reachable` en `renderBoard()`; línea muerta `btn-rematch` eliminada. Validado: sintaxis node OK (game.js y script del admin), JSON de reglas OK, simulación standalone de fillText/escape.
- **v0.3.26 — caza de bugs (5 fixes):** (1) **Chat online mudo** — regresión de `eceb631`: `Chat.mount()` asignaba `Net.onChatMessage` y DESPUÉS llamaba `Net.stopChat()`, que además de soltar el listener hace `onChatMessage=null` → ningún mensaje se mostraba (ni propio ni del rival). Fix: `stopChat()` primero, callback después. (2) **Toasts del anillo sin i18n** — `applyCellEffect()` armaba el toast con template hardcodeado en vez de `fillText('toastRingBig'/'toastRingDrip')` (las claves existían en TEXTS_ES/EN pero nadie las usaba; tampoco tomaban overrides de /admin/). (3) **CPU "desesperada" casi toda la partida** — `cpuDesperateHpMin: 30` se calibró con maxHp 100 y no se reescaló en el recorte a 35 (v0.3.08): la CPU entraba en modo desesperado con 30/35 = 86% de vida (arriesgaba pase 1 y apretaba puntería casi siempre). Fix: nuevo `cpuDesperateHp()` interpreta el valor como **% del maxHp real del rival** (30% — para rivales de 100 HP el comportamiento es idéntico al original); usado en `cpuDecideMove()` y `scheduleCpuStop()`. Simulado en node: umbrales 10.5/3/60/30 para maxHp 35/10/200/100. (4) **Modo Caos se filtraba al torneo x4** — `mode-t4` y `OT.beginMatch()` solo apagaban Paredes (`if(App.wallsMode)`): con Caos activado antes, el board viajaba como `C~…` (rompía el `sqrt(len)` del espectador y metía Caos en partidas ajenas). Fix: ambos apagan cualquier modo especial. (5) **Globos "Vos"/"Rival" hardcodeados** — en inglés salían en español; nuevas claves `bubbleYou`/`bubbleRival` en TEXTS_ES/EN + `TEXT_PARAMS` de /admin/ sincronizado. Validado: sintaxis node OK; sin cambios de reglas de DB.
- **v0.3.25 — 2 skins nuevas (Sumi, Kai):** al catálogo `SKINS` se suman `sumi` (`sprites/sumi.webp`) y `kai` (`sprites/kai.webp`), convertidas de PNG con Pillow. Solo data (nueva entrada + asset); el picker y el render ya soportan N skins. Total ahora: Clásica, Malla, Sumi, Kai. Verificado con Chrome headless (las 3 sprites renderean centradas, mismo tamaño, sin fondo azul).
- **v0.3.24 — fix: la skin propia se veía azul sólido en partida (menú ok):** el sprite `is-opp` (rival) andaba pero el `is-you` (skin del jugador) salía como bola azul sin imagen. Causa: la regla de torneo online `.cell .player-marker.is-you{ background: var(--you-accent, var(--accent)) }` (línea ~1166) usa el **shorthand `background:`**, tiene la MISMA especificidad que `.has-sprite` y va DESPUÉS en el archivo → rompía el pintado del `background-image` del sprite (el `is-opp` no tiene esa regla tardía, por eso andaba). Fix: esa regla pasa a `background-color:` (solo el color, que es lo único que quería tocar). Además, defensivo: `.has-sprite` ahora pinta la imagen vía `background-image: var(--sprite-url) !important` + `background-color: transparent !important`, y el JS setea `--sprite-url` (en vez de `style.backgroundImage`) en `is-you`, `is-opp` y el swap de movimiento de `flipMarker()`. Verificado con Chrome headless (harness con la CSS real linkeada): computed `background-image` correcto en ambos y render visual con la malla del jugador + tarata del rival + bola default, todas centradas y del mismo tamaño. LECCIÓN: nunca usar el shorthand `background:` en una regla que pueda coincidir con `.has-sprite`/`.has-skin` — resetea el `background-image`; usar `background-color`.
- **v0.3.23 — fix: fichas (y skins) descentradas + skin demasiado grande:** Lucio reportó la skin "muy grande y descentrada". Causa raíz: `.cell .player-marker` es `position:absolute` SIN `top/left` y se confiaba en el `display:flex; align-items/justify-content:center` del `.cell` para centrarlo — pero flexbox NO centra un hijo `position:absolute` (queda arriba-izquierda). Se notaba poco con la bola default y saltó a la vista con el sprite. Y `.has-sprite` estaba en 82% (vs 60% de la bola) → se veía enorme. Fix: centrado explícito por offset `left/top:20%` en el marker base (bola 60%) y `left/top:11%` en `.has-skin` (emoji 78%); `.has-sprite` a 60% (igual que la default). Sin `transform` (lo reservan el FLIP y el `.is-clash`). Verificado con Chrome headless (harness aislado con la CSS real): bola default, malla, tarata, bandera Messi y bola rival — todas centradas y del mismo tamaño. LECCIÓN: el `.cell` centra por flex solo a hijos en flujo; cualquier `.player-marker`/overlay `position:absolute` necesita su propio `left/top` (no heredar el centrado del flex).
- **v0.3.22 — cambio de skin de la ficha propia (perfil, solo registrados):** en el overlay de cuenta (modo `session`), bajo las stats, nuevo selector: preview de la bola al centro + flechas ◀▶ a los costados, nombre debajo. Catálogo `SKINS` (`{id,name,nameEn,sprite}`) — por ahora `default` (bola sólida `--accent`) y `malla` (`sprites/malla.webp`, convertida de PNG con Pillow). `Profile` guarda la skin en `localStorage.rally_skin` (aplica al instante) y, si hay sesión, en `users/{uid}/skin` (sigue a la cuenta entre dispositivos; Firebase gana al abrir el perfil vía `Profile.loadRemote()`). Nuevo `G.spriteYou`, seteado en `resolveSkins()` **solo si `User.name`** (perk de cuenta: logueado sí, anónimo no); `renderBoard()` pinta la ficha `is-you` con `.has-sprite` (mismo mecanismo que el sprite del rival), con el emoji Messi todavía con prioridad. `database.rules.json`: validación `skin` bajo `users/$uid` (`[a-z0-9_]{1,20}`). i18n: label estático `user-skin-label` (ES 'Tu ficha' / EN 'Your token'), nombre de skin por `LANG`. **Testeo:** lógica de ciclado/validación simulada en node (wrap ok, ids inválidos → default); **no se pudo verificar en browser** (esta sesión no tiene Chromium bundled ni node_modules). Pendiente confirmar visualmente con Lucio: preview + flechas en el perfil logueado, y la ficha con malla en partida.
- **v0.3.21 — sprite de movimiento para Tarata:** nuevo `G.spriteOppMove` (url o null) + `NPC_SPRITES['Tarata']` ahora es `{idle, move}` en vez de string plana; nodo 0 de campaña trae `opp.spriteMove:'sprites/tarata_move.webp'`. `flipMarker()` (el FLIP de casilla a casilla) detecta cuando anima `is-opp` y, si hay `spriteOppMove`, cambia el `background-image` del marker al sprite de movimiento apenas arranca el deslizamiento y lo vuelve a `G.spriteOpp` (idle) en `transitionend`. Sin variante de movimiento en el nodo/NPC_SPRITES → se queda con el idle todo el trayecto, no rompe nada (mismo patrón defensivo que NPC_SPRITES v0.3.20). Asset: `public/sprites/tarata_move.webp` (128x128, convertido de PNG subido por Lucio vía Pillow — `sips` de macOS no exporta a webp). Aplica solo al rival de campaña (`is-you` no tiene sprites todavía).
- **v0.3.20 — sprite de Tarata, causa raíz probable + fix real:** el v0.3.18/19 ponía el sprite en `CAMPAIGN_SCRIPT[0].opp.sprite`, pero `applyRemoteCampaign()` REEMPLAZA la cinta entera con `campaign/script` de Firebase si existe (este repo clonado apunta al MISMO proyecto `rallye-online` que el original) — los nodos remotos del editor no traen `sprite`, así que `G.spriteOpp` quedaba null aunque el código y el deploy estuvieran bien. Fix: nuevo mapa `NPC_SPRITES` (nombre de rival → url) como fallback en `applyOppCosmetic()`: `o.sprite || NPC_SPRITES[o.name] || null`. Funciona venga el nodo del código o del editor, mientras el rival se llame "Tarata". LECCIÓN: cualquier campo cosmético nuevo en nodos de campaña tiene que sobrevivir el override remoto (o agregarse también al editor de /admin/).
- **v0.3.19 — sprite de Tarata no se veía (fix defensivo):** Lucio probó v0.3.18 en `rallyyy-test.web.app` y el marker de Tarata seguía apareciendo como círculo sólido, sin la imagen. Revisado `CAMPAIGN_SCRIPT`/`applyOppCosmetic()`/`renderBoard()`, todo correcto en el código y en el deploy (verificado leyendo el commit ya pusheado). **No se pudo confirmar en runtime** si `sprites/tarata.webp` devuelve 200 o 404 — esta sesión remota no tiene salida de red hacia `rallyyy-test.web.app` ni hacia el RTDB para inspeccionar directo. Se detectó una diferencia real igual: `.has-sprite` no tenía `background-color: transparent` como sí tiene `.has-skin` — si la imagen no pintara por el motivo que sea, el color sólido de `.is-opp`/`.is-you` queda tapando todo el marker. Agregado por las dudas. **Pendiente de confirmar con el usuario:** abrir DevTools → Network en la partida de campaña y ver el status code de la request a `sprites/tarata.webp`.
- **v0.3.18 — sprite de imagen para el primer NPC de campaña (prueba):** hasta ahora `has-skin` solo pintaba un emoji de texto (`G.skinOpp`) sobre el `.player-marker`. Se agregó soporte paralelo para sprites de imagen real: `G.spriteOpp` (url o null) + clase `.has-sprite` (`background-image`, `background-size:cover`, sin el glow blanco de `::after`). `applyOppCosmetic()` lo setea desde `node.opp.sprite` cuando el nodo activo de campaña es de tipo `match`, y lo limpia (`null`) fuera de campaña (torneo, práctica, online) para no filtrarse a otros modos. Nodo 0 de campaña (Tarata) ahora trae `sprite:'sprites/tarata.webp'`. Sin tocar `G.skinYou`/`G.skinOpp` (el easter egg Messi sigue igual); el sprite tiene prioridad sobre el skin de emoji si ambos estuvieran seteados. Prueba de concepto — solo el rival, no el jugador.
- **v0.3.17 — fix "la barra de duelo no va fluida" (reporte de Lucio):** las dos veces anteriores que se tocó esto (v0.2.95, luego `eceb631`) ya habían eliminado el layout-thrashing (transform en vez de `left`, ancho del track + nodos de las agujas cacheados una sola vez por duelo) — confirmado con Playwright que el rAF del duelo corre sólido a ~16.6ms/frame incluso con CPU throttling 6x, sin excepciones ni el loop trabándose. La causa real encontrada: `G.duel.trackWidth` se cacheaba UNA vez al arrancar el duelo (`buildSpeedometer()`) y nunca se actualizaba — si el ancho disponible cambiaba a mitad de duelo (rotar el celu, la barra del navegador mobile apareciendo/desapareciendo, zoom, split-screen), la aguja seguía moviéndose contra el ancho VIEJO mientras las zonas de color (hechas en %) sí seguían el ancho real → aguja desalineada/"corriéndose" de las zonas, lo cual se percibe como falta de fluidez. Reproducido con Playwright (`page.setViewportSize` a mitad de duelo: el track real bajó a 356px pero la aguja seguía calculando su posición contra 380px cacheados). Fix: `ResizeObserver` sobre `.speedo-track` (creado una sola vez, persiste entre duelos ya que el track nunca se destruye) que mantiene `G.duel.trackWidth` al día en vivo; la medición síncrona en `buildSpeedometer()` se conserva para el valor inicial correcto desde el primer frame. Sin cambios en la lógica de puntaje (`timeToPosition`/`computeScore` intactos). Validado: mismo test de resize ahora muestra el ancho cacheado convergiendo al real (~2px de margen por la naturaleza async del observer) en vez de quedar 24px desalineado; duelo completo (offline, auto-resolución a las 4 pasadas) sigue terminando en `duel-reveal` sin errores de consola.
- **v0.3.16 — bloque de crédito en "Cómo se juega":** `.page-credit` más chico (11px → 9px) y se agregó un link a `https://guerra-sur.web.app/` debajo de "Creado por lucio" (mismo tratamiento en TEXTS_EN vía `STATIC_I18N_EN['page-credit']`).
- **🌐 v0.3.15 — idioma ES/EN:** ícono de globo en `top-controls` (misma estética que el toggle luna/sol, mismo criterio de visibilidad: oculto en pantalla de partida). Si el jugador nunca eligió idioma, se detecta `navigator.language` ANTES del primer paint (script inline en `index.html`, mismo patrón que el tema): español → `es`, cualquier otro → `en` por defecto. Persiste en `localStorage.rally_lang`. Arquitectura: `TEXTS_ES` (el objeto de siempre, sigue recibiendo overrides remotos de `/admin/`) + `TEXTS_EN` (traducción estática, sin override remoto — **limitación conocida**: el panel de admin solo edita texto en español) + `TEXTS` como copia de trabajo repoblada por `refreshTexts()` según `LANG`. El HTML estático que nunca pasaba por `TEXTS` (botones, labels, placeholders) ahora tiene `id` y se traduce vía `STATIC_I18N_EN` + `applyStaticLang()` (cachea el español del propio DOM la primera vez). El Lab (`/admin/` testing) no se tradujo completo — bajo impacto, uso interno. Validado con Playwright: detección automática a inglés, toggle ES↔EN, persistencia post-reload, pantallas home/offline/lobby/info.
- **🔎 SEO — Verificación Search Console + meta tags:** propiedad `rallyyy.web.app` verificada vía archivo HTML (subido a `public/`, no a la raíz). `<title>` → "Rally - Partidas Online" + nuevo `<meta name="description">`. **Pendiente:** repuesta la cuota diaria, pedir indexación manual de `https://rallyyy.web.app`; evaluar `sitemap.xml` si se suman más rutas.
- **v0.3.14 — barra de duelo, 2do intento:** eliminado el costo de pintado restante del duelo: `perfectShine` ahora anima solo `opacity` vía `::after` (no `box-shadow`), y `body.is-dueling` oculta (`visibility:hidden`) todo el juego debajo del overlay durante el duelo. Validado 9/9.
- **v0.3.13 — barra de duelo lageada (Modo Caos):** las animaciones CSS infinitas de portales/bombas del Modo Caos competían con el rAF de la aguja. Fix: `setDuelOverlayShown(on)` pausa todos los loops decorativos del board durante el duelo. Validado 6/6.
- **v0.3.12 — MODO CAOS Fases 2-3 (final):** Bomba (arma al pisar, detona 2 turnos después, daño en área con piedad), Terreno alto (+2 daño en duelo), Botas (próximo movimiento a radio 2), CPU adaptada a los nuevos ítems, grupo "Modo Caos" en Lab/admin. Validado 20/20.
- **v0.3.11 — MODO CAOS Fase 1 (beta):** nuevo modo offline+online 1v1 excluyente con Paredes. Cofre sorpresa (daño/def/cura/trampa/teleport) y Portales (par, teleportan al gemelo). Viaja en el board string con prefijo `C~`, cero cambios a `database.rules.json`. Validado 30/30.
- **v0.3.10 — bug serie bo3 online:** el resultado de ronda se pasaba en <1s y la ronda 2 quedaba bugeada. Causa: `Net.listenStart()` disparaba con el board VIEJO ya en Firebase. Fix: `_lastBoardStr` congela el valor visto y solo un board distinto dispara `onStart`. Validado 19/19.
- **v0.3.09-08 — Partidas rápidas + serie mejor de 3:** `CFG.maxHp` 100→35 (Torneo offline queda en 100), serie online default "mejor de 3" (`BO5_TARGET` 3→2), puntos de ronda visuales, favicon nuevo, fix de "100 HP" hardcodeado visible antes de cargar, fix de cache (Firebase Hosting cacheaba el HTML hasta 1h → `no-cache` en `**/*.html`).
- **v0.3.04-07:** aviso visual de duelo inminente (onda azul entre casillas contiguas), limpieza de menú principal, ajuste de timing de la onda para no pisar animación de movimiento.
- **v0.2.97-3.03 — Editor del juego en `/admin/`:** editor de mapas (grilla clicable con paredes, valida conectividad BFS), editor de campaña (nodos escena/partida con mapa embebido), editor de personajes (roster de 8), Lab restringido a admins, animación de choque en misma casilla, textos del juego editables (`TEXTS`, 163 claves) con reglas y panel admin, refactor de duplicación de código (v0.2.98), fix botón de tema pisando HUD, pantalla de resultado de torneo mejorada (dorado/pulso/sonido/recap de HP).
- **v0.2.83-96 — Panel de admin + stats + visitas:** panel `/admin/` con login, acceso restringido a admins (bootstrap self-claim), sección para agregar admins, workflow `grant-admin.yml` (Admin SDK), estadísticas de jugador (`users/{uid}/stats`, 6 contadores online-only), contador de visitas del sitio, fix de stats que no se guardaban (auth async), fix de nombre sin límite de caracteres, animación FLIP para movimiento entre casillas (incl. diagonales), fix aguja de duelo trabada (`transform` en vez de `left`), fix mini-speedómetro del "Cómo se juega" (mismo patrón), globos "Vos"/rival al arrancar partida, espectador de torneo online ve el duelo en curso (no solo resultado).
- **v0.2.6x-0.2.82 y anteriores:** auth anónima + reglas definitivas de Firebase (primera publicación real, deploy automático de reglas y hosting vía GitHub Actions desde entonces), login real con usuario/contraseña, chat online, modo oscuro, modo Paredes (beta, sin test online real), campaña offline y torneo offline (8 rivales roster Madagascar), torneo online x4 (`OT`, nunca verificado end-to-end con dispositivos reales), Laboratorio de testing (`?lab=1`), fixes de login que se cerraba solo y "sala llena" fantasma.

## LECCIONES (leer antes de tocar código)
- **⚠️ Rendimiento del duelo — la "barra lageada" ya apareció 4 veces. Reglas acumuladas:** (1) NUNCA animar en loop `box-shadow`/`filter`/`left`/`top` — solo `transform`/`opacity` (+`will-change`); si hace falta pulsar una sombra, sombra ESTÁTICA + `::after` animando opacity. (2) Toda animación infinita nueva en una celda del board se suma al bloque `body.is-dueling{animation-play-state:paused}` de style.css. (3) Durante el duelo el juego de abajo queda `visibility:hidden` — si algo DEBE verse durante el duelo, va ADENTRO de `.duel-overlay`. (4) El toggle del overlay vive en `setDuelOverlayShown()` — nunca tocar `duel-overlay`/`is-show` directo. (5) El throttle de CPU de CDP NO reproduce jank de raster de móvil — no confiar en "acá se ve fluido" para descartar un reporte de celu.
- **`[hidden]` vs CSS:** si un contenedor con `hidden` tiene `display:` en CSS de autor, agregar SIEMPRE `[hidden]{display:none}` explícito.
- **Centrado en el board:** el `.cell` es flex-center, pero eso NO centra hijos `position:absolute` (`.player-marker` y overlays quedan arriba-izquierda). Centrar por offset propio `left/top:(100-tamaño)/2 %`, nunca por `transform` (lo reservan el FLIP y `.is-clash`).
- **Sprites de ficha (`.has-sprite`):** la imagen se pinta con `background-image: var(--sprite-url) !important` (el JS setea `--sprite-url`). NUNCA usar el shorthand `background:` en reglas que puedan coincidir con `.has-sprite`/`.has-skin` (ej. el tinte de torneo `.is-you`) — el shorthand resetea `background-image` y rompe el sprite; usar `background-color`. Y todo sprite que se pinte por primera vez DENTRO de una animación corta (ej. variante de movimiento en el FLIP de 0.35s) necesita precarga (`new Image().src`) al arrancar la partida, o su primera aparición se pierde mientras descarga (v0.3.28).
- **Rings/bordes en móvil:** `box-shadow: inset`, no hacia afuera (se recorta).
- **Verificar merge siempre:** después de mergear, chequear `git log origin/main` + diff contra la rama — no asumir que tomó todo.
- **Cache HTML/JS:** de ahí el `?v=` por versión — mantenerlo al bumpear.
- **Versionado:** el usuario copia y renombra la carpeta de versión — es por carpeta, sin importar la herramienta.
- **Namespace del emulador Firebase:** `rallye-online-default-rtdb`, NO `rallye-online` (usar el corto crea un namespace fake sin reglas, falso positivo silencioso).
- **`.transaction()` de Firebase necesita permiso de LECTURA además de escritura** — si se agrega otro contador global con `.transaction()`, agregar su `.read` puntual explícito o falla en silencio.
- **`users/{uid}` es de lectura solo-dueño** — para mostrar info de OTROS usuarios (ej. nombres de admins), usar `usernames/` (legible por cualquier autenticado) invertido en el cliente, nunca leer `users/{uid}` ajeno.
- **Mantener sincronizados:** `PARAMS`/`TEXT_PARAMS` (admin) deben reflejar siempre `LAB_PARAMS`/`TEXTS` (game.js) — mismas claves y defaults.

## REGLAS FIJAS DE WORKFLOW (no negociables)
1. **Mergear SIEMPRE automáticamente** al entregar un update — no dejar el PR abierto esperando aprobación.
2. **No entregar HTML fusionado.** Con el merge a `main` alcanza: el deploy automático a Firebase Hosting lo publica.
3. **Actualizar este archivo SIEMPRE al terminar cada actualización, ANTES de mergear** — el merge nunca va sin este archivo al día.
4. **Revisar/actualizar `database.rules.json` en la MISMA entrega** cuando el cambio toca algo que las reglas validan — no dejarlo para después (el deploy de reglas es automático en cada push a `main`).

## WORKFLOW (sesión remota)
- Se trabaja sobre 3 archivos split en `public/` (index.html, style.css, game.js), commits/push a rama `claude/...` y PR a `main`.
- Por versión: 1) editar, 2) bump `VERSION` en game.js + `<title>` y `.version-tag` en index.html + los `?v=` de `<script>/<link>` (cache-busting), 3) validar sintaxis con `node -e "new Function(...)"` sobre game.js.
- Screenshots: playwright-core + Chromium, viewport 390px.
- Cambios de matemática (daño/puntería/probabilidad): simular standalone en node ANTES de entregar.
- Si el usuario trae un `.html` fusionado (viene de Design/VS Code): re-splitear y comparar VERSION contra la última conocida.

## DEPLOY
- **✓ Automático (desde 2026-07-02):** `.github/workflows/firebase-deploy.yml` deploya a Firebase Hosting en cada push/merge a `main`.
- **✓ Reglas de DB también automáticas (desde 2026-07-04):** `firebase-rules-deploy.yml` corre en cada push a `main` que modifique `database.rules.json`.
- Site: **`rallyyy`** (`firebase.json` target `rally` → `.firebaserc` → `rallyyy`). `rallyyy-test` no se usa.

## FIREBASE
- Proyecto `rallye-online`, Realtime Database. Reglas reales en producción desde v0.2.89 (antes test-mode).
- El proxy remoto bloquea `*.web.app`/`*.firebaseio.com` pero sí `identitytoolkit.googleapis.com`. Testear reglas siempre con emulador local.
- Panel admin `/admin/`: acceso restringido a admins, bootstrap self-claim si `admins/` está vacío, sección para agregar otros admins, sin remoción desde la UI (solo consola/workflow `grant-admin.yml`).

## COLABORACIÓN EXTERNA (revisado 2026-07-03)
Un amigo del usuario dijo haber creado "una rama" — verificado en GitHub, no existe (solo `main` + ramas `claude/...`, único colaborador `guerrasur`). Probable fork sin push aún. Próxima sesión: pedir su usuario de GitHub o link.

**Idioma:** todo con el usuario (Lucio) en español argentino.
Memoria entre sesiones: mantener COMPACTO (el usuario cuida tokens — condensar/borrar lo viejo al agregar secciones nuevas).

## BACKLOG (repriorizado 2026-07-10)
- **i18n:** overrides remotos de `/admin/` (`texts/`) solo aplican en español; Lab sin traducir completo (uso interno).
- **Endurecer validación de campos compuestos en `database.rules.json`:** `moves`, `ejects`, `spec.A/B` no fuerzan que el valor sea un objeto (`hasChildren`). Bajo impacto, no urgente.
- **📈 Visitas del sitio:** hecho (v0.2.85). A futuro: distinguir visitas nuevas vs. recurrentes, visitas por día/semana.
- **Estadísticas de jugador:** hecho (v0.2.84). A futuro: sistema real de logros, daño hecho/recibido en perfil del jugador, ranking/leaderboard.
- **Login real:** hecho (v0.2.79-80). A futuro: migrar progreso de localStorage a `users/$uid`; email real/Google para recupero de contraseña.
- **Panel de admin:** hecho y endurecido. A futuro: dominio/site propio (hoy `rallyyy.web.app/admin/`), remoción de admins desde la UI, historial de cambios de config.
- **Testear en vivo (2 dispositivos):** torneo online x4 (OT) end-to-end y Modo Paredes online — ninguno de los dos fue verificado con dispositivos reales todavía.
- **#19 Torneo:** curación extra entre rondas, animación al subir puesto, rivales ocultos hasta desbloquear.
- **#18 Modo Veneno:** no arrancado.
- **Campaña:** escribir nodos nuevos (infraestructura lista).
- **SEO:** sitemap.xml pendiente si se agregan más rutas navegables.

## PREFERENCIAS DEL USUARIO
- Visuales mínimos e integrados (bajar efectos llamativos).
- Testea offline entre cambios; deploya vía merge automático. Cuida tokens: incremental, un set de cambios por vez, validar antes de entregar.
- Le gusta que le ofrezcan opciones en decisiones de diseño/mecánica, y herramientas de testeo (Lab, spawnear anillo).
- Suele mandar varios pedidos juntos; hacerlos "de una" si son chicos, validando igual incrementalmente. Mantener y repriorizar este backlog.
